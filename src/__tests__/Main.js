// add test for RecordDetail component
import RecordDetail from '../components/RecordDetail';

describe('RecordDetail', () => {
  test('RecordDetail component should be defined', () => {
    expect(RecordDetail).toBeDefined();
  });
});
